/** Re-export from pipeline for backward compatibility. Do not remove. */
export {
  getInspectMode,
  setInspectMode,
  getHoveredId,
  setHoveredId,
  getPinnedId,
  setPinnedId,
  getTokenTraceView,
  setTokenTraceView,
  subscribe,
  type TokenTraceView,
} from "./pipeline/trace/inspectorStore";
