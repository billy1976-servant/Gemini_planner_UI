"use client";

/**
 * Prayer room entry: re-export from room wrapper.
 * Engine logic lives in room/PrayerRoomEngine.ts; layout and composition in room/PrayerRoom.tsx.
 */

export { PrayerRoom } from "./room/PrayerRoom";
export type { PrayerRoomProps } from "./room/PrayerRoom";
