{
  "description": "Card layout preset ID → required content slots. Layouts that show media require image slot.",
  "layoutRequirements": {
    "image-top": { "requires": ["image"] },
    "image-left": { "requires": ["image"] },
    "image-right": { "requires": ["image"] },
    "image-bottom": { "requires": ["image"] },
    "centered-card": { "requires": [] },
    "centered-image-left": { "requires": ["image"] },
    "centered-image-right": { "requires": ["image"] }
  }
}
