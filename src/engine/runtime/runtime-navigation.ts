/**
 * Runtime Navigation Executor
 * No UI logic. No guessing.
 */


export function navigate(detail: any) {
    if (!detail) return;
  
  
    // Future: screen routing, modal routing, browser routing
    // For now, state-based navigation is handled via Action → state
  }
  
  
  